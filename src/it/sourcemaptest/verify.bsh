import java.io.*;

File file = new File( basedir, "target/coffee/cookbook.js" );
if ( !file.isFile() ) {
  throw new FileNotFoundException( "Could not find generated joinset javascript file: " + file );
}

file = new File( basedir, "target/coffee/cookbook.js.map" );
if ( !file.isFile() ) {
  throw new FileNotFoundException( "Could not find generated joinset sourcemap file: " + file );
}
